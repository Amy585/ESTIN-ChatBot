# 🧠 FAQ Chatbot (React + FastAPI + NLP)

A simple chatbot that answers frequently asked questions using NLP intent classification.  
Frontend built with **React**, backend built with **FastAPI** and **scikit-learn**.